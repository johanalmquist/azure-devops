resources:
  repositories:
    - repository: templates
      name: Aranya-Devops-Templates/Aranya-Devops-Templates
      type: git

trigger:
  branches:
    include:
      - main
      - develop

pr: none

pool: ara-devops-agents-01

extends:
  template: build_and_deploy.yaml@templates
  parameters:
    appName: "my-app"
    useTerraform: false
